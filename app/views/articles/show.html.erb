<p id="notice"><%= notice %></p>
<div class="post-page" style="">
<div class="post-col" style="">
	<a href="/">На главную</a>
	<!--<div class="post-page-header" style="background:url('assets/space2.jpg')" id="<?php echo $posts[$id]['type']; ?>">
		
		<h1 class="header-text" style="color: rgb(245, 245, 245);">imonlyone</h1>

	</div>-->
	<div class="innerText">
		<div class="date-added"><%#=@article.created_at %></div>
		<p><%= @article.content %></p>
</div>

<%@article.comments.each do |comment|%>
	<%=User.find(comment.user_id).email%>
	<%=comment.body%>
<%end%>


<h2>Add a comment:</h2>
<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  
  <p>
    <%= form.hidden_field :article_id,:value=>4 %>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>

<!--<p id="notice"><%= notice %></p>
<%=  @article.inspect %>
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @article.content %>
</p>

<p>
  <strong>Image:</strong>
  <%= @article.image %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
-->